# Flat Wide Email Design

Increase the shared desktop email width from 640px to 720px while retaining `width:100%` responsiveness. Center the existing hero logo within the wider canvas. Update the preview scaler to use the same 720px source width and remove the preview card's rounded corners and drop shadow so the email appears as a flat document surface. All template content, colors, and mobile behavior remain unchanged.

